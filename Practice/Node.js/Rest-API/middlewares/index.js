const errorHandler = require('./errorHandler');
const notFound = require('./notFound');
const validateBody = require('./validateBody');
const auth = require('./auth');

module.exports = { errorHandler, notFound, validateBody, auth };
